
# git 
git add . && git commit -m "backup" && git push

# docker
docker compose -f docker-compose.yml up -d --build

docker logs -f macjarvis-backend --tail 100


# 代理
HTTP代理: 
http://host.docker.internal:7897
HTTPS代理: 
http://host.docker.internal:7897

