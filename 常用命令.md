
# 项目介绍
这是一个Mac Agent，可以执行各类mac操作。

# git 
git add . && git commit -m "backup" && git push

# 启动
./start_prod.sh

# 代理
HTTP代理: 
http://host.docker.internal:7897
HTTPS代理: 
http://host.docker.internal:7897

# .env文件位置
.env文件已经配置好各类需要的API KEY，路径：/Users/xinfuzhang/Desktop/Code/mac_agent/.env
你可以在其他代码中加载使用，以及调整代码中的.env文件路径避免出错
包含以下变量：
```bash
OPENROUTER_BASE_URL="https://openrouter.ai/api/v1"
OPENROUTER_API_KEY=...
OPENAI_API_KEY=...
DISABLE_MEMORY_MANAGER=true
```

