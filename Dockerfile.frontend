# 前端 Dockerfile
# 功能：构建 MacJarvis 前端应用镜像
# 路径：项目根目录

FROM node:18-alpine AS builder

WORKDIR /app

# 复制前端文件
COPY frontend/package*.json ./
RUN npm ci

COPY frontend/ ./

# 构建前端应用（使用相对路径，由 nginx 代理）
RUN npm run build

# 使用 nginx 提供静态文件服务
FROM nginx:alpine

# 复制构建产物到 nginx
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
